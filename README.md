# Guidora Shopify Theme Branch

This branch exists so Shopify can detect a valid Liquid theme from GitHub.

The main Guidora website is a Next.js app on the `main` branch. Shopify Themes cannot use a Next.js app directly, so this branch contains a minimal Shopify-compatible theme structure.

No products, prices, cart logic, checkout buttons, fake reviews, or fake catalog data are included.
